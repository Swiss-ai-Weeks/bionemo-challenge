# NVIDIA NGC Catalog key. Actual credentials belong only in .env.
NGC_API_KEY=

# Web app uses one GPU by default. OpenFold3 is optional.
BOLTZ_GPU_ID=0
OPENFOLD_GPU_ID=1
APP_PORT=8080
APP_BIND_ADDRESS=127.0.0.1
